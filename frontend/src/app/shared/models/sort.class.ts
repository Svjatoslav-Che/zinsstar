// Enum to filter offers by
export enum SortType {
  Land = 'land',
  Percent = 'percent',
  Duration = 'duration',
  Bank = 'bank'
}

// Order
export enum OrderType {
  none = '-',
  decs = 'decs',
  ase = 'ase'
}
